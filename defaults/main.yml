# General configuration

bind_hostname: ""
bind_domain_name: "local"
bind_ip: ""
bind_listen_on: any
bind_listen_on_v6: none

# Master Configuration

bind_is_master: no
bind_master_entries : []
bind_slave_hosts: []
bind_mail: ""
bind_notify: no

# Slave configuration

bind_is_slave: no
bind_master_host: ""

# Cache configuration

bind_is_caching: no
bind_first_forwarder: ""
bind_second_forwarder: ""
