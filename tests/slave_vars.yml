domain_name: local

is_master: no             #'yes' or 'no'
master_entries : [
    {"name": "www", "ip":"127.0.0.1"}
]
slave_hosts: [ "127.0.0.1" ]

is_slave: yes             #'yes' or 'no'. If both is_slave and is_master are set on yes, the node will be a master.
master_host: 127.0.0.1

is_cache: no              #'yes' or 'no'
first_forwarder: 8.8.8.8   
second_forwarder: 8.8.4.4   
